# docker-compose.yml
# Para testes locais. No Easypanel, configure como App > Docker.

version: "3.8"

services:
  brokers-scraper:
    build: .
    container_name: brokers-lead-exporter
    env_file: .env
    volumes:
      - ./exports:/app/exports
    restart: "no"
